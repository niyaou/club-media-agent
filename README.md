# Club Media Agent

This repository is organized by task.

Each top-level task folder is self-contained and owns its own files, notes, prompts, and implementation details. Task folders should not depend on shared project-level subfolders for their internal workflow.

Current sections:

- `browser-style-crawl/`: browser-driven crawling work
